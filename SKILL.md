---
name: chwippohaja
description: 한국 취업 지원 환경의 초기 설정부터 공고 등록, JD 분석, 기업 리서치, 지원서 입력, 자소서 작성과 검토, Google Drive 보관, Notion 상태 관리, 최종 제출 자료 정리까지 이어서 수행한다. 처음 취업 자료 환경을 만들거나 새 회사 지원을 시작하거나 기존 지원 작업을 재개할 때 사용한다. 일반적인 진로 상담이나 면접 연습만 요청한 경우에는 사용하지 않는다.
metadata:
  short-description: 한국 취업 지원 전 과정을 Drive와 Notion으로 관리
---

# 취뽀하자

한국 취업 지원 한 건의 상태를 발견하고, 필요한 단계만 이어서 진행한다. 공고, 사실 원본, 기업 맞춤 문서, 증빙, 실제 제출 자료를 서로 섞지 않는다.

## 시작

1. 현재 작업 위치와 상위 폴더에서 `.chwippohaja/workspace.json`을 찾는다.
2. 표식이 없으면 현재 구조를 점검한다. 취업 자료 구조도 없으면 초기 설정 모드로 전환하고, 기존 구조가 있으면 새로 만들지 말고 가져오기 여부를 확인한다.
3. 설정된 워크스페이스에서는 표식의 활성 시즌, `profile_status`와 연결 상태를 확인하고 이 스킬의 규칙을 기본으로 사용한다. 로컬 `AGENTS.md`나 운영 문서가 있으면 해당 환경의 추가 규칙으로 읽되 없어도 정상적으로 진행한다.
4. 이력서, 지원서 또는 자소서 작성 요청인데 상태가 `empty`면 정보 설정을 먼저 진행한다. 공고 등록과 JD 분석만 요청한 경우에는 정보 설정을 건너뛸 수 있다.
5. 회사명, 직무, 공고, 마감일과 현재 지원 단계를 대화와 기존 파일에서 확인한다.
6. 기존 회사 폴더와 Notion 공고가 있으면 완료된 단계를 재실행하지 않는다.
7. 작성 범위가 필요한 요청은 `자소서만`과 `지원서 전체`를 구분한다. 사용자가 범위를 명시했으면 다시 묻지 않고, 모호할 때만 선택하게 한다.
8. 아래 실행 모드 중 현재 요청에 필요한 모드만 선택하고 해당 참고 문서만 읽는다.

회사명이나 직무가 현재 작업에 꼭 필요하지만 대화와 파일에서 확인할 수 없을 때만 사용자에게 묻는다. 이미 제공된 정보를 다시 질문하지 않는다.

## 실행 모드

| 모드 | 대표 요청 | 필수 참고 문서 |
| --- | --- | --- |
| 초기 설정 | 처음 설정, 워크스페이스 생성, 기존 환경 가져오기 | [온보딩](references/onboarding.md) |
| 정보 설정 | 기존 이력서 가져오기, 첫 프로필 작성, 공통 사실 준비 | [정보 온보딩](references/profile-onboarding.md) |
| 공고 등록과 기업 조사 | 공고 저장, JD 분석, 기업 리서치, 기업 폴더와 Notion 등록 | [공고와 연결 서비스](references/posting-and-connectors.md) |
| 자소서만 | 자소서 문항 수집, 소재 선정, 초안, 리뷰, 윤문 | [자소서 작성과 검토](references/essay-workflow.md), [공고와 연결 서비스](references/posting-and-connectors.md) |
| 지원서 전체 | 전체 항목 스캔, 복사·붙여넣기용 지원서 작성본과 자소서 작성본 분리, 증빙과 첨부 준비, 요청 시 공식 폼 자동 입력 | [지원서와 증빙](references/application-and-evidence.md), [자소서 작성과 검토](references/essay-workflow.md) |
| 소재 정리 | 프로젝트, 협업, AI 활용 경험 보강 | [자소서 작성과 검토](references/essay-workflow.md) |
| 제출 정리 | 제출 결과 확인, 공식 제출본과 접수 증빙 확보, Drive 링크와 Notion 완료 | [제출과 최종 점검](references/submission-and-checklist.md) |
| 전체 진행 | 신규 공고부터 제출 정리까지 | 모든 참고 문서를 단계별로 읽기 |

폴더 구조, 사실 원본과 버전 상태를 판단해야 하면 [워크스페이스와 상태](references/workspace-and-state.md)를 먼저 읽는다.

같은 기업의 여러 공고, 공고 변경, 마감 예외, 법인 구분 또는 연결 실패 재처리가 발생하면 [예외 시나리오](references/exception-scenarios.md)를 읽는다.

## 절대 지침

- 사실 원본에는 사용자에게 확인된 개인 사실만 저장한다.
- 기존 문서에서 추출했지만 사용자가 확인하지 않은 사실은 공통자료에 저장하지 않는다.
- 기업명, JD 해석, 지원 전략, 소재 선택과 글자 수 조건은 기업별 작업 문서에만 저장한다.
- 여러 지원서에 반복되는 학력, 교육, 경력, 자격, 어학과 수상은 사용자가 확인한 공통 사실로 관리한다. 회사별 요구에 맞춘 표현과 선택은 공통자료에 역으로 합치지 않는다.
- 확인되지 않은 사실, 수치, 기간과 역할을 추측하지 않는다. 필요한 자리는 `TBD`로 남긴다.
- 템플릿을 참고하라는 요청은 구조와 항목만 참고한다. 예시 내용이나 주변 문서의 개인정보를 가져오지 않는다.
- 증빙이 필요한 수상, 자격과 경력은 실제 서류의 존재와 제출 조건을 확인한다.
- 실제 제출 폼 전송, 외부 메시지 발송과 공유 권한 변경은 사용자가 명시적으로 요청한 범위에서만 수행한다.
- 작성 중 파일이나 PDF의 존재만으로 제출을 추정하지 않는다. 사용자의 실제 제출 확인 또는 스킬이 확인한 공식 접수 완료 결과가 있어야 지원 단계를 `지원 완료`로 바꾼다.
- 제출 PDF를 제공하지 않는 사이트에서는 PDF를 만들었다고 보고하지 않는다. 공식 접수증, 완료 화면이나 접수 상태를 확보하고 PDF 미제공 사실을 기록한다.
- Notion이나 Drive 반영이 요구된 작업에서 연결 또는 권한 문제로 반영하지 못하면 완료로 보고하지 않는다.
- 토큰, 쿠키, 계정 이메일, 데이터베이스 ID, 파일 ID와 절대 경로를 스킬 파일이나 공개 저장소에 기록하지 않는다.
- 초기 설정은 생성 계획과 보존할 기존 파일을 먼저 보여주고 사용자의 승인을 받은 뒤 적용한다.
- 초기 설정과 기존 환경 가져오기에서 기존 파일을 덮어쓰지 않는다.
- `전체 자소서 문항`을 `지원서 전체 항목`으로 확대 해석하지 않는다.
- 공식 지원 페이지의 지원서 초안 생성, 입력 저장과 제출은 사용자가 해당 범위를 명시적으로 선택하기 전에는 수행하지 않는다.
- 지원서 전체 구조를 가져올 때 이미 채워진 입력값은 수집하지 않는다. 필드명, 필수 여부, 선택지, 형식과 첨부 조건만 확인하며 민감정보의 실제 값은 작성 파일, Notion과 보고에 기록하지 않는다.
- `지원서_항목.md`는 화면 구조 문서이며 공식 폼 입력 원본으로 사용하지 않는다. 실제 입력에는 사용자가 채택한 `지원서_작성본.md`와 `자소서_작성본.md`만 사용한다.
- 공식 폼 입력은 사용자가 명시적으로 요청한 경우에만 수행한다. 값 입력, 임시저장과 최종제출은 서로 다른 권한으로 취급하며 하나의 요청을 다음 단계의 승인으로 확대하지 않는다.
- 공식 폼에는 `확정` 값만 입력한다. `TBD`와 `사용자 직접 입력`은 건너뛰고, 기존 값은 내용을 수집하지 않은 채 비어 있음, 동일함 또는 충돌함만 판정한다. 충돌값을 덮어쓰려면 사용자 확인을 받는다.
- 공고에 의해 미리 지정되는 지원 직무는 일반 입력값으로 취급하지 않는다. 현재 페이지의 공고 식별자와 회사 및 직무 의미를 우선 비교하고, 괄호, 신입과 경력 같은 채용 구분, 공백 또는 부문 접두사처럼 표시 형식만 다른 경우에는 충돌로 판정하지 않는다. 의미가 같으면 시스템 지정값을 그대로 유지한다.
- 입력과 동시에 자동 저장되는 사이트는 입력 자체를 외부 저장으로 취급하고 실행 전에 이를 알린 뒤 승인을 받는다.
- 자소서 답변은 지원 화면이 허용하는 한 첫 줄에 명사형 `[핵심 요약 문구]` 하나를 둔다. `했습니다`, `합니다`, `입니다`처럼 서술형으로 끝내지 않는다. 이 문구도 글자 수에 포함해 검증하며 사용자가 다른 형식을 지정하면 그 형식을 우선한다.
- 자소서 파일을 만들었거나 글자 수를 맞췄다는 이유만으로 초안이 완성됐다고 보고하지 않는다. 문항의 모든 필수 하위 질문, 소재 적합성, 사실 근거와 본인 역할을 검토해 최소 기준을 충족한 경우에만 `유효 초안`으로 판정한다.
- 문항과 소재의 연결이 약하면 기술, 산업 또는 키워드가 비슷하다는 이유로 끼워 맞추지 않는다. 더 적합한 소재를 제안하거나 필요한 사실을 확인하고, 해결되지 않으면 `부분 초안` 또는 `보완 필요`로 명시한다.
- 지원서에 붙여 넣을 자소서 본문은 기업이 개조식을 요구하지 않는 한 완결된 문장과 문단으로 이어지는 서술식으로 작성한다. 작성 설계와 검토 기록에는 개조식을 사용할 수 있지만 이를 답변 본문에 그대로 복사하지 않는다.
- 경험 문항은 성과나 기술 나열로 끝내지 않는다. 결과에서 실제로 도출되는 배운 점, 느낀 점, 달라진 판단 기준 또는 이후 행동을 구체적으로 밝히며 일반적인 교훈을 새로 만들어 붙이지 않는다.
- Notion 사용을 선택했는데 지원 관리 데이터베이스가 없으면 부모 페이지와 생성 계획을 확인한 뒤 데이터베이스를 만든다. 달력 보기는 선택 사항이며 별도 데이터베이스를 만들지 않는다.
- 지원서 전체 모드에서도 자소서 답변과 자소서 외 입력값을 하나의 파일로 합치지 않는다. 두 작성본은 독립적으로 버전 관리하고 연결한다.
- 지원서 전체 모드의 기본 완료 지점은 공식 화면 순서대로 값을 채운 복사·붙여넣기용 Markdown 작성본이다. 공식 폼 자동 입력은 사용자가 별도로 요청한 경우에만 선택적으로 수행한다.
- 지원서 작성본의 미확정 항목은 필수, 선택, 조건부와 사용자 직접 입력으로 묶어 한 번에 확인한다. 한 필드씩 질문하고 매번 새 버전을 만드는 흐름을 기본으로 사용하지 않는다.
- 지원서 전체 구조를 확보하면 공통자료의 확인된 사실을 필드 의미와 형식에 맞춰 새 `지원서_작성본.md`에 자동 반영한다. 원본 공통자료와 `지원서_항목.md`는 수정하지 않고, 매핑되지 않은 필드는 `TBD` 또는 `사용자 직접 입력`으로 남긴다.
- 지원서 작성본을 만들기 전에 최상위 섹션뿐 아니라 행 추가 화면과 조건부 하위 필드까지 스캔한다. 필수 여부, 선택지, 입력 형식, 단위, 날짜 범위와 연동 필드를 확인하지 못한 항목은 추정하지 않고 구조 문서에 미확인으로 남긴다.
- 공고 변경과 재제출에서도 이전 원본과 제출 파일을 덮어쓰지 않는다.
- 로컬 `AGENTS.md`와 별도 운영 문서는 선택 사항이다. 초기 설정에서 자동 생성하지 않고 기존 파일이 있으면 보존한다.
- 도구가 만든 임시 작업 폴더를 워크스페이스나 시즌 폴더에 남기지 않는다. 보존할 가치가 있는 검토 결과만 해당 기업의 `03_검토기록`에 저장한다.

## 도구와 의존 기능

- Google Drive 작업은 연결된 Drive 기능으로 파일을 먼저 찾고 실제 메타데이터에서 URL을 얻는다. URL을 추측해 만들지 않는다.
- Notion 작업은 검색 후 대상 페이지와 데이터 소스의 현재 스키마를 가져온 다음 수정한다.
- 연결 상태를 확인하거나 변경할 때는 `scripts/workspace_setup.py integration-status`를 사용한다.
- PDF, 스프레드시트와 문서 파일은 해당 형식 전용 기능이 있을 때 그 기능을 우선한다.
- 제출 이후에는 현재 인증된 공식 지원 페이지에서 다운로드와 인쇄 기능을 먼저 확인한다. 로그인은 사용자가 직접 수행하며 접근 제한을 우회하지 않는다.
- 공식 제출 화면은 열 수 있지만 운영체제의 PDF 저장 대화상자를 제어할 수 없으면 같은 자동화를 반복하지 않는다. 현재 운영체제에 맞는 최소 수동 저장 단계와 정확한 대상 경로를 안내하고, 저장된 파일의 탐색과 후속 검증부터 자동으로 재개한다.
- 작성 중 내용 원본은 기본적으로 Markdown으로 보관한다. 사용자가 다른 원본 형식을 선택하면 그 선택을 따른다.
- 지원 건 폴더 생성과 다음 작성 번호 계산은 `scripts/application_workspace.py`를 사용한다.
- 자소서 초안과 모든 수정본은 `scripts/validate_essay_style.py`로 가운데점, 영어 표현 후보, 연결어미 뒤 쉼표, 대괄호 핵심 요약의 서술형 종결과 본문의 개조식 행을 검사한다. 검사 결과는 자동 합격 판정이 아니라 빠짐없이 검토할 후보 목록이다. 문항과 소재의 적합성 및 배운 점의 타당성은 별도의 의미 검토로 확인한다.
- `humanize-korean`이 있으면 내용 검증이 끝난 자소서의 보수적 윤문에만 사용한다. 시스템 임시 폴더에서 실행하고 결과를 곧바로 제출본으로 취급하지 않는다.
- 선택적 의존 기능을 사용할 수 없으면 안전한 대체 검토를 수행하고 미사용 사실을 보고한다. 연결이 필수인 단계는 중단한다.

## 작업 진행

1. 워크스페이스가 없으면 초기 설정하거나 기존 구조를 가져온다.
2. 개인 사실이 필요한 요청이면 정보 상태를 확인하고 필요한 범위만 정리한다.
3. 공고와 JD를 확보하고 회사별 작업 공간을 만든 뒤 기업과 직무를 조사한다.
4. Notion을 사용하면 기존 지원 관리 데이터베이스를 선택하고, 없으면 승인된 부모 페이지에 새 데이터베이스를 만든 뒤 공고 항목을 생성하거나 갱신한다. 달력 보기는 사용자가 원할 때만 같은 데이터베이스에 추가한다.
5. 선택한 범위가 자소서만이면 자소서 문항만 확보한다. 지원서 전체이면 현재 입력값을 제외하고 최상위 섹션, 행 추가 화면, 조건부 하위 필드, 선택지, 입력 형식, 단위, 날짜 제한과 첨부 조건까지 먼저 확보한다.
6. 지원서 전체이면 `지원서_항목.md`의 화면 순서를 유지한 새 `지원서_작성본.md`에 공통자료의 확인된 사실과 붙여넣기 값을 매핑한다. 자소서는 별도 자소서 작성본으로 유지하고, 민감정보 입력란은 `사용자 직접 입력`, 매핑되지 않은 항목은 `TBD`로 남긴다. 논리적 입력 행의 상태 집계와 별도로, 각 행 안의 필수 하위 필드가 화면 구조와 모두 대응됐는지 검증한다. 필수 하위 필드가 하나라도 누락되면 행 전체를 `확정`으로 처리하지 않는다.
7. 문항을 하위 질문으로 분해하고 각 하위 질문을 직접 증명하는 소재와 근거를 대응시킨다. 필수 하위 질문에 근거가 없거나 소재가 부적합하면 초안을 억지로 완성하지 말고 소재를 바꾸거나 보완 정보를 확인한다.
8. 번호가 붙은 기업별 작성본을 만들고 사용자 피드백에 따라 새 버전을 보존한다. 문항별 최소 유효성 기준을 통과하지 못한 문서는 `부분 초안` 또는 `보완 필요`로 표시한다.
9. 사실, 문항과 소재의 적합성, 문항 충실도, 직무 연결, 본인 역할, 소재 중복, 경험에서 도출된 배움, 구조와 표현 순서로 검토한다. 표현 검토에서는 전용 검사기가 찾은 모든 가운데점, 영어 표현 후보, 서술형 대괄호 요약과 개조식 본문 후보를 유지, 설명, 교체 또는 삭제 중 하나로 판정하고 근거를 남긴다.
10. 필요한 경우 한국어 윤문을 수행하고 원문 대비 의미와 수치를 다시 검증한다. 윤문본이나 표현 수정본에도 전용 표현 검사를 다시 실행한다.
11. 사용자가 공식 폼 자동 입력을 요청하면 채택한 두 작성본, 입력할 확정값, 건너뛸 항목과 첨부 자료를 먼저 확인한 뒤 화면에 입력한다. 기존 값과 충돌한 필드는 덮어쓰지 않고 건너뛰며, 안전하게 계속 확인할 수 있으면 다른 섹션의 충돌과 누락도 모아 저장 전에 한 번에 보고한다.
12. 입력 후 항목별 오류, 글자 수, 필수 누락과 첨부 결과를 다시 확인한다. 요청받지 않은 임시저장이나 최종제출은 실행하지 않는다.
13. 제출이 확인되면 공식 지원 페이지에서 제출 지원서 다운로드, 인쇄와 접수증을 순서대로 확인해 가능한 자료를 확보한다. 운영체제의 PDF 저장 대화상자가 차단되면 정확한 경로로 사용자가 한 번 저장하게 하고, 완료 후 새 PDF를 찾아 이름과 위치를 정리한다.
14. 실제 제출 파일은 PDF 전용 기능으로 페이지 수, 첫 페이지와 마지막 페이지, 빈 페이지와 잘림을 검증한다. 검증된 제출 파일과 접수 증빙을 최종 제출 폴더에 보관하고 Drive와 Notion을 갱신한다.

각 단계가 끝날 때 산출물, 남은 사용자 확인 사항과 외부 서비스 미반영 여부를 짧게 보고한다.

## 보고 형식

- 사용자가 문장부호나 문체 선호를 정했으면 작성본뿐 아니라 진행 보고에도 적용한다.
- 가운데점은 공식 고유명사나 출처 원문을 보존할 때만 사용한다.
- 표는 짧은 값의 비교에만 사용하고 헤더와 열 수를 확인한다. 긴 설명이나 여러 문장이 들어가면 목록을 사용한다.
- 검토 이슈는 고유 번호를 부여하고 여러 검토 영역에서 같은 문제를 다시 언급해도 전체 개수에 중복 산입하지 않는다.

## 완료 기준

- 요청된 로컬 파일이 올바른 폴더에 있다.
- 초기 설정 단계라면 표식과 기본 구조를 다시 확인했다.
- 정보 설정 단계라면 추출 후보와 확인된 사실을 구분했고 상태 변경을 다시 확인했다.
- 작성 중 문서는 버전 규칙을 지킨다.
- 기업 맞춤 문장이 공통 사실 원본을 오염시키지 않는다.
- 기업 리서치의 주요 사실에 기준일과 출처가 있고 해석을 사실처럼 단정하지 않았다.
- 글자 수와 고유명사, 수치와 역할을 다시 확인했다.
- 자소서 작성 단계라면 각 필수 하위 질문에 확인된 근거가 대응되고 주 소재가 문항에서 요구한 행동이나 역량을 직접 증명한다. 필수 내용이 비었거나 소재가 부적합한 문서는 `완료`, `유효 초안`, `최종 후보`로 보고되지 않았다.
- 자소서 작성 단계라면 경험 문항의 결과 뒤에 실제 사례에서 도출된 배운 점, 느낀 점, 달라진 판단 기준 또는 이후 행동이 있고 기술과 성과의 나열로 끝나지 않는다.
- 자소서 작성 단계라면 최신 작성본에 전용 표현 검사를 실행했고, 요약은 명사형이며 본문은 별도 요구가 없는 한 서술식이다. 가운데점은 공식 고유명사나 인용 원문 외에는 남지 않았으며 생소한 영어 약어와 내부 구현 용어는 쉬운 한국어로 바꾸거나 첫 등장에 설명했다.
- 요청된 Drive 링크는 실제 파일 메타데이터로 검증했다.
- 요청된 Notion 속성과 본문을 다시 가져와 반영 결과를 확인했다.
- 지원서 전체 단계라면 자소서 작성본과 자소서 외 지원서 작성본이 분리되어 있고 각각의 Drive 링크가 대응하는 Notion 링크에 연결되어 있다.
- 공통자료에서 지원서 작성본을 만들었다면 각 자동완성 값의 출처가 확인되고 회사별 정보가 공통자료에 섞이지 않았으며 기존 작성본을 덮어쓰지 않았다.
- 지원서 전체의 기본 요청이라면 사용자가 화면 순서대로 복사해 입력할 수 있는 작성본이 완성됐고 공식 폼 자동 입력은 수행하지 않았다.
- 미확정 항목과 자동 입력 충돌은 한 필드씩 반복 질문하지 않고 범주별 확인 묶음이나 통합 오류 목록으로 보고됐다.
- 지원서 작성본의 각 논리적 입력 행에서 화면상 필수 하위 필드가 모두 매핑됐고, 누락된 필드가 있는 행은 `확정`으로 집계되지 않았다.
- 지원서 항목 구조를 수집했다면 기존 입력값과 민감정보가 산출물이나 보고에 포함되지 않았다.
- 지원서 항목 구조에서 필수 여부와 화면 입력 형식을 확인하지 못한 하위 필드가 명시적으로 미확인 처리됐다.
- 공식 폼 입력 단계라면 채택한 작성본의 `확정` 값만 입력했고, 건너뛴 항목과 화면 검증 오류를 보고했으며, 별도 요청 없는 저장이나 최종제출을 실행하지 않았다.
- 최종 제출 단계라면 실제 제출 지원서 PDF가 제공되는 경우 이를 확보해 연결했다. 제공되지 않는 경우에는 접수 증빙과 PDF 미제공 사실이 기록되어 있고, `최종 제출본`을 다른 파일로 대신 채우지 않았다.
- 사용자가 수동으로 저장한 제출 PDF라면 새 파일을 정확히 식별해 최종 제출 폴더로 이동했고, PDF 전용 기능의 페이지 판정과 Drive 메타데이터를 기준으로 검증했다.
