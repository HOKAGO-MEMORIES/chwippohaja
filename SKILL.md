---
name: chwippohaja
description: 한국 취업 지원의 공고 등록과 JD 분석, 확인된 경험을 바탕으로 한 자소서 및 지원서 작성, 버전 보관과 제출 결과 정리를 수행한다. 신규 지원을 준비하거나 기존 지원 작업을 재개할 때 사용한다. 일반적인 진로 상담이나 면접 연습만 요청한 경우에는 사용하지 않는다.
metadata:
  short-description: 한국 취업 지원 전 과정을 Drive와 Notion으로 관리
---

# 취뽀하자

공고와 확인된 개인 사실을 근거로 지원 자료를 작성하고, 사용자가 채택한 버전과 실제 제출 자료를 보관한다. 저장 방식은 로컬 또는 Google Drive 동기화 폴더이며 Notion은 지원 상태와 링크의 인덱스다.

사용자에게는 **공고 확인과 기업 조사 → 자소서 작성 → 자소서 외 지원서 작성 → 제출 정리**의 네 단계로 설명한다. 요청 범위에 필요하지 않은 단계는 생략한다. 자소서가 없는 공고에는 빈 자소서 파일을 만들지 않는다.

## 시작과 재개

1. `scripts/workspace_setup.py discover --path "현재 작업 경로"`로 가장 가까운 워크스페이스와 활성 시즌을 찾는다. 기존 구조가 있으면 보존해 가져오며 초기화하지 않는다. 설정 경로에 접근할 수 없으면 다른 경로로 자동 대체하지 않는다.
2. 기존 로컬 `AGENTS.md`와 운영 문서는 추가 규칙으로 읽는다. 없으면 생성하거나 필수로 요구하지 않는다.
3. 현재 요청의 회사, 직무, 공고와 범위를 대화 및 기존 파일에서 확인한다. 명시된 정보를 다시 묻지 않는다. `전체 자소서 문항`은 지원서 전체 입력 항목을 뜻하지 않는다.
4. 작성에 필요한 개인 사실이 없다면 정보 설정을 진행한다. 공고 등록과 조사만 할 때는 개인 정보 설정을 건너뛸 수 있다.
5. [워크스페이스와 상태](references/workspace-and-state.md)를 읽고 현재 기준 파일, 훅 실행 상태와 남은 외부 반영을 확인한다. 최신 번호와 채택본을 동일시하지 않는다. 완료된 작업은 반복하지 않는다.
6. 아래에서 현재 모드의 참고 문서만 읽는다. 전체 진행도 단계가 바뀔 때 해당 문서를 읽는다.

## 모드별 참고 문서

| 모드 | 범위 | 읽을 문서 |
| --- | --- | --- |
| 초기 설정 | 새 워크스페이스 또는 기존 환경 가져오기 | [온보딩](references/onboarding.md) |
| 정보 설정 | 기존 이력서 가져오기와 공통 사실 준비 | [정보 온보딩](references/profile-onboarding.md) |
| 공고 등록과 조사 | 원문, JD, 기업 리서치와 상태 등록 | [공고와 연결 서비스](references/posting-and-connectors.md), 훅 사용 시 [조사 훅](references/research-hooks.md) |
| 자소서·소재 정리 | 문항 분석, 경험 선택, 작성과 검토 | [자소서 작성](references/essay-workflow.md), [체크포인트](references/essay-checkpoints.md), [문항별 규칙](references/essay-rules.md), 훅 사용 시 [자소서 훅](references/essay-hooks.md) |
| 지원서 전체 | 공식 화면 구조와 복사·붙여넣기용 작성본, 증빙 | [지원서와 증빙](references/application-and-evidence.md); 자소서가 있으면 자소서 모드 문서도 읽기 |
| 제출 정리 | 실제 제출 확인, 제출본과 접수 증빙 보관 | [제출과 최종 점검](references/submission-and-checklist.md) |

자소서 문항을 새로 수집할 때는 [공고와 연결 서비스](references/posting-and-connectors.md)의 출처 선택을 적용한다. 같은 기업의 복수 공고, 변경 공고, 마감과 상태 충돌 또는 연결 복구는 [예외 시나리오](references/exception-scenarios.md)를 읽는다.

## 공통 원칙

- 공통자료에는 사용자에게 확인된 개인 사실만 저장한다. 추출 후보는 확인 전까지 분리하고, 기업별 표현과 전략을 사실 원본으로 역반영하지 않는다. 확인되지 않은 수치, 기간, 역할, 감정과 동기는 만들지 않는다.
- 템플릿은 구조만 참고한다. 예시나 주변 문서의 개인정보를 가져오지 않는다. 연락처, 생년월일, 상세 주소와 등록번호 등의 실제 값은 공통자료와 작성본에 저장하지 않고 `사용자 직접 입력`으로 표시한다. 세부 범위는 지원서 참고 문서를 따른다.
- 공고, JD, 공고 분석, 기업 리서치와 문항 원본은 기업별 `01_공고_JD`에 보관한다. Notion은 상태와 실제 외부 링크를 관리하며 상세 본문 복사는 사용자가 요청했을 때만 한다.
- 확인된 사실, 기업 맞춤 작업본, 검토 기록과 실제 제출 자료를 분리한다. 자소서와 자소서 외 지원서 작성본도 각각 버전 관리한다. 파일 생성과 다음 작성 번호는 `application_workspace.py`를 사용한다.
- 초기 설정과 기존 환경 가져오기는 생성 계획을 확인한 뒤 적용하고 기존 파일을 덮어쓰지 않는다. 새 지원 건 생성 등 이미 요청에 포함된 작업의 범위와 권한은 다시 묻지 않는다.
- 실제 폼 입력, 임시저장과 최종제출의 권한을 구분한다. 공식 폼 자동 입력은 명시적인 요청이 있을 때 수행하며 지원서 전체의 기본 결과는 복사·붙여넣기용 Markdown이다. 자동 저장과 기존 값 충돌은 지원서 참고 문서의 절차를 따른다. 이미 승인된 동일 범위는 반복 승인받지 않는다.
- 개인 사실과 제출 조건은 필수 검증 대상이다. 소제목과 문체는 기본값이며 기업 또는 사용자의 형식 요구가 우선한다. 예외는 문항별 규칙에 근거와 함께 기록하고 같은 규칙으로 검사한다.
- 문항에 맞는 경험이 없으면 해당 문항을 보류한다. 다른 문항의 유효 답변은 부분초안으로 보존하고 확인 질문을 묶어 묻는다. 현재 자료로 고칠 수 있는 문제는 수정하고 재검사한다.
- 사용자 채택본을 수정의 기준으로 삼는다. 사실 오류, 역할 과장, 필수 질문 누락과 제출 조건 위반은 바로잡고, 필수 기준이 충족되면 취향 수준의 수정을 반복하지 않는다. 소재와 의미 검토의 기준은 자소서 작성 참고 문서 한 곳에서 관리한다.
- 작성본이나 PDF가 있다는 이유로 제출을 추정하지 않는다. 사용자 확인 또는 공식 접수 완료 근거가 있어야 `지원 완료`로 기록한다. 실제 미지원은 `지원 안 함`, 제출 뒤 전형 종료나 철회는 `종료`다.
- 요청 대상 전체를 추적한다. 실패나 미시도 대상을 누락하거나 다른 기업으로 교체하지 않는다. 전수·최신 탐색의 범위와 집계는 공고 참고 문서에 따라 확인한다.
- 도구 성공 응답만으로 완료를 보고하지 않는다. 저장본, 화면 또는 외부 메타데이터를 다시 읽어 요청 범위를 확인한다. 로컬 검증, 외부 반영과 실제 제출은 서로 다른 상태다.
- 토큰, 쿠키, 계정 정보, 외부 서비스 ID, 개인 자료와 로컬 절대 경로를 공개 스킬 저장소에 넣지 않는다. 연결 서비스의 URL을 추측하지 않고 실제 메타데이터로 확인한다. 공유 권한 변경과 외부 메시지는 명시적으로 요청된 범위에서만 수행한다.
- PDF와 문서 등의 편집·검증에는 해당 형식 전용 기능을 우선한다. 임시 윤문 파일은 시스템 임시 폴더에서 처리하고 보존할 검토 결과만 기업별 `03_검토기록`에 둔다.

## 실행과 검증

### 공고 확인과 기업 조사

기업별 자료를 모두 작성한 뒤 `validate_research_stage.py --application "지원 건"`으로 현재 분석과 리서치를 검사한다. 문서가 여러 개면 `application_state.py select`로 기준 파일을 명시하며 과거 파일은 보존한다. 검사 통과는 최소 구조 확인이고 사실 및 출처의 의미 검토를 대신하지 않는다.

조사 훅을 사용하면 최초 대상 전체를 `research_hook.py start`에 등록한다. Drive나 Notion 반영까지 요청받았으면 `--require-service`로 포함한다. 외부 조회로 반영을 확인한 뒤 `application_state.py sync`에 근거를 기록한다. 미완료 사유를 해결하면 `resume`, 모두 완료되면 `finish`와 최종 집계 검사를 수행한다.

### 자소서 작성

작성 순서는 **문항에 대한 쉬운 설명과 소재 적합성 확인 → 본문에 필요한 근거 선택 → 초안 → 문항과 본문만으로 독해 검토 → 원본 대조와 형식·분량 검사**다. Codex가 자료로 먼저 판단하며 사용자의 실제 경험이나 가치관이 부족할 때만 질문한다. 판단 결론과 근거를 짧게 기록하고 내부 문답 전체를 사용자에게 출력하지 않는다. 세부 기준과 실패 예시는 [자소서 작성](references/essay-workflow.md)을 따른다.

새 설계와 주요 수정본은 체크포인트 `schema_version: 2`로 작성한다. 하위 질문별 근거 연결과 `writing_brief`를 남기고 `pre_draft` 검사 후 별도 답변 파일을 작성한다. 답변 블록에는 문항 ID를 붙인다. `reader_review`에 본문에서 읽히는 답과 독해 이슈 처리 결과를 기록한 뒤 `draft_review`와 표현·분량을 검사한다. 체크박스는 검토 결과이며 내용 품질을 증명하지 않는다. 기존 템플릿이 구형이어도 새 문서에는 현재 스키마를 적용한다.

훅이나 외부 연결이 없어도 이 작성·검토 순서를 수행한다. 스크립트를 실행할 수 없는 환경에서는 같은 항목을 수동 대조하고 본문을 제공하되 자동 검사와 미확인 분량은 미실행으로 보고한다. 자동 검사 통과를 뜻하는 값은 임의로 true로 바꾸지 않는다.

훅 사용 시 `essay_hook.py start`에 전체 문항과 필요한 `--rules`를 등록한다. `plan-check` 후 작성하고 `check`와 `finish`로 검증한다. 같은 지원 건의 새 버전은 `advance`, 다른 건으로 전환할 때는 `suspend`로 기존 실행을 보존한다. 상세 명령은 자소서 훅 참고 문서를 따른다.

사용자가 채택한 파일은 `application_state.py select --kind essay`로 기록한다. 파일 선택 자체는 품질 검증이나 제출 확인이 아니다. 윤문은 내용 검증 뒤 선택적으로 진행하고 변경된 의미와 수치를 다시 대조한다.

### 지원서 작성과 제출 정리

지원서 전체는 현재 입력값을 제외한 공식 화면 구조를 먼저 확인하고, 각 필수 하위 필드를 공통자료의 사실과 대응시킨다. 구조를 확인하지 못했거나 값이 부족한 행은 확정 처리하지 않는다. 작성본 선택은 `application_state.py select --kind application`으로 기록한다.

제출이 확인되면 공식 제출본과 접수 증빙을 확보하고 전용 기능으로 검증한다. 운영체제 인쇄 대화상자 자동화가 차단되면 반복 시도하지 않고 정확한 수동 저장 경로를 안내한 뒤 파일 검증부터 재개한다. 공식 지원서 PDF를 제공하지 않는 사이트에서는 미제공 사실과 가능한 증빙을 기록한다.

## 보고와 완료

요청 범위의 산출물, 채택한 버전, 완료된 검증, 남은 사용자 확인과 외부 미반영을 짧게 보고한다. 사용자 문체 선호는 보고에도 적용한다. 긴 설명을 표에 넣지 않고 검토 이슈는 고유 번호로 집계한다.

여러 대상은 전체, 완료, 부분 완료, 실패와 미시도 수가 최초 대상 수와 일치해야 한다. 하나라도 남았으면 전체 완료로 표현하지 않는다. 로컬 전용 환경은 외부 연결이 없다는 이유만으로 미완료로 처리하지 않는다. 외부 반영까지 요청받았다면 해당 재조회가 끝나기 전에는 전체 완료로 보고하지 않는다.

모드별 완료 기준은 해당 참고 문서를 따른다. 파일 존재, 글자 수나 체크포인트 통과만으로 내용의 정확성, 문항 적합성 또는 실제 제출을 주장하지 않는다.
